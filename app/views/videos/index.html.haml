%article.video_category
  - @categories.each do |category|
    %header
      %h3= category.name.capitalize
    .videos.row
      - category.recent_videos.each do |video|
        .video.col-sm-2
          = link_to video do
            = image_tag("/tmp/#{video.small_cover_url}.jpg")
